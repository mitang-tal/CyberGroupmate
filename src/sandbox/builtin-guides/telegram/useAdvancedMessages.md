<!-- AUTO-GENERATED by src/tools/generate-telegram-mtcute-guides.ts. Do not edit this file manually. -->
TelegramGuide: useAdvancedMessages
## 高级消息
用于不适合常驻 brief 的消息流程：转发、复制、评论、引用、定时消息、网页预览、reaction 用户和消息关联查询。
调用本 guide 方法只会把本文注入上下文；真正执行时直接调用下面的 `telegram.<method>(...)`。这些方法会由 adapter 通过同名 mtcute high-level method 转发给当前登录的 mtcute client。
## 使用要点
- `answer*`、`reply*`、`comment*` 需要已有 mtcute `Message` 对象；如果只有 message id，先用常驻 `getMessages` 或相应检索 API 获取消息对象。
- 转发、复制、评论、引用和定时发送都会改变聊天内容，调用前确认目标 chat。
## mtcute high-level API
### telegram.answerText
Send a text to the same chat (and topic, if applicable) as a given message
```ts
telegram.answerText(message: Message, ...params: ParametersSkip2<typeof sendText>): ReturnType<typeof sendText>;
```
### telegram.answerMedia
Send a media to the same chat (and topic, if applicable) as a given message
```ts
telegram.answerMedia(message: Message, ...params: ParametersSkip2<typeof sendMedia>): ReturnType<typeof sendMedia>;
```
### telegram.answerMediaGroup
Send a media group to the same chat (and topic, if applicable) as a given message
```ts
telegram.answerMediaGroup(message: Message, ...params: ParametersSkip2<typeof sendMediaGroup>): ReturnType<typeof sendMediaGroup>;
```
### telegram.commentText
Send a text comment to a given message.

If this is a normal message (not a channel post),
a simple reply will be sent.

**Available**: ✅ both users and bots

@throws MtArgumentError
    If this is a channel post which does not have comments section.
    To check if a post has comments, use Message#replies.hasComments
```ts
telegram.commentText(message: Message, ...params: ParametersSkip2<typeof sendText>): ReturnType<typeof sendText>;
```
### telegram.commentMedia
Send a text comment to a given message.

If this is a normal message (not a channel post),
a simple reply will be sent.

**Available**: ✅ both users and bots

@throws MtArgumentError
    If this is a channel post which does not have comments section.
    To check if a post has comments, use Message#replies.hasComments
```ts
telegram.commentMedia(message: Message, ...params: ParametersSkip2<typeof sendMedia>): ReturnType<typeof sendMedia>;
```
### telegram.commentMediaGroup
Send a text comment to a given message.

If this is a normal message (not a channel post),
a simple reply will be sent.

**Available**: ✅ both users and bots

@throws MtArgumentError
    If this is a channel post which does not have comments section.
    To check if a post has comments, use Message#replies.hasComments
```ts
telegram.commentMediaGroup(message: Message, ...params: ParametersSkip2<typeof sendMediaGroup>): ReturnType<typeof sendMediaGroup>;
```
### telegram.forwardMessages
Forward one or more Messages to another chat.

> **Note**: all messages must be from the same chat.
**Available**: ✅ both users and bots
```ts
telegram.forwardMessages(params: ForwardMessageOptions & {
        messages: Message[];
    }): Promise<Message[]>;
```
### telegram.forwardMessagesById
Forward one or more messages by their IDs.
You can forward no more than 100 messages at once.

**Available**: 👤 users only

@param toChatId  Destination chat ID, username, phone, `"me"` or `"self"`
@param fromChatId  Source chat ID, username, phone, `"me"` or `"self"`
@param messages  Message IDs
@param params  Additional sending parameters
@returns  Newly sent, forwarded messages in the destination chat.
```ts
telegram.forwardMessagesById(params: ForwardMessageOptions & {
        /** Source chat ID, username, phone, `"me"` or `"self"` */
        fromChatId: InputPeerLike;
        /** Message IDs to forward */
        messages: number[];
    }): Promise<Message[]>;
```
### telegram.sendCopy
Copy a message (i.e. send the same message, but do not forward it).

Note that if the message contains a webpage,
it will be copied simply as a text message,
and if the message contains an invoice,
it can't be copied.
**Available**: ✅ both users and bots
```ts
telegram.sendCopy(params: SendCopyParams & ({
        /** Source chat ID */
        fromChatId: InputPeerLike;
        /** Message ID to forward */
        message: number;
    } | {
        message: Message;
    })): Promise<Message>;
```
### telegram.sendCopyGroup
Copy a message group (i.e. send the same message group, but do not forward it).

Note that all the provided messages must be in the same message group
**Available**: ✅ both users and bots
```ts
telegram.sendCopyGroup(params: SendCopyGroupParams & ({
        /** Source chat ID */
        fromChatId: InputPeerLike;
        /** Message IDs to forward */
        messages: number[];
    } | {
        messages: Message[];
    })): Promise<Message[]>;
```
### telegram.quoteWithText
Send a text in reply to a given quote
```ts
telegram.quoteWithText(message: Message, params: QuoteParamsFrom<Parameters<typeof sendText>[3]> & {
        /** Text to send */
        text: Parameters<typeof sendText>[2];
    }): ReturnType<typeof sendText>;
```
### telegram.quoteWithMedia
Send a media in reply to a given quote
```ts
telegram.quoteWithMedia(message: Message, params: QuoteParamsFrom<Parameters<typeof sendMedia>[3]> & {
        /** Media to send */
        media: Parameters<typeof sendMedia>[2];
    }): ReturnType<typeof sendMedia>;
```
### telegram.quoteWithMediaGroup
Send a media group in reply to a given quote
```ts
telegram.quoteWithMediaGroup(message: Message, params: QuoteParamsFrom<Parameters<typeof sendMediaGroup>[3]> & {
        /** Media group to send */
        medias: Parameters<typeof sendMediaGroup>[2];
    }): ReturnType<typeof sendMediaGroup>;
```
### telegram.replyText
Send a text in reply to a given message
```ts
telegram.replyText(message: Message, ...params: ParametersSkip2<typeof sendText>): ReturnType<typeof sendText>;
```
### telegram.replyMedia
Send a media in reply to a given message
```ts
telegram.replyMedia(message: Message, ...params: ParametersSkip2<typeof sendMedia>): ReturnType<typeof sendMedia>;
```
### telegram.replyMediaGroup
Send a media group in reply to a given message
```ts
telegram.replyMediaGroup(message: Message, ...params: ParametersSkip2<typeof sendMediaGroup>): ReturnType<typeof sendMediaGroup>;
```
### telegram.getDiscussionMessage
Get discussion message for some channel post.

Returns `null` if the post does not have a discussion
message.

This method might throw `FLOOD_WAIT_X` error in case
the discussion message was not *yet* created. Error
is usually handled by the client, but if you disabled that,
you'll need to handle it manually.

**Available**: 👤 users only

@param peer  Channel where the post was found
@param message  ID of the channel post
```ts
telegram.getDiscussionMessage(params: InputMessageId): Promise<Message | null>;
```
### telegram.getFactCheck
Get fact check information for one or more messages in a chat

**Available**: 👤 users only

@param chatId  Chat where the messages are located
@param msgIds  One or more message IDs
```ts
telegram.getFactCheck(chatId: InputPeerLike, msgIds: MaybeArray<number>): Promise<(FactCheck | null)[]>;
```
### telegram.getMessageByLink
Given a message link (e.g. `t.me/durov/1`), fetch the relevant message.
**Available**: ✅ both users and bots
```ts
telegram.getMessageByLink(link: string): Promise<Message | null>;
```
### telegram.getMessageGroup
Get all messages inside of a message group

**Available**: ✅ both users and bots

@param chatId  Chat ID
@param message  ID of one of the messages in the group
```ts
telegram.getMessageGroup(params: InputMessageId): Promise<Message[]>;
```
### telegram.getReactionUsers
Get users who have reacted to the message.

**Available**: 👤 users only

@param params
```ts
telegram.getReactionUsers(params: InputMessageId & {
        /**
         * Get only reactions with the specified emoji
         */
        emoji?: InputReaction;
        /**
         * Limit the number of users returned.
         *
         * @default  100
         */
        limit?: number;
        /**
         * Offset for pagination
         */
        offset?: GetReactionUsersOffset;
    }): Promise<ArrayPaginated<PeerReaction, GetReactionUsersOffset>>;
```
### telegram.iterReactionUsers
Iterate over users who have reacted to the message.

Wrapper over getReactionUsers.

**Available**: ✅ both users and bots

@param chatId  Chat ID
@param messageId  Message ID
@param params
```ts
telegram.iterReactionUsers(params: Parameters<typeof getReactionUsers>[1] & {
        /**
         * Limit the number of events returned.
         *
         * @default  `Infinity`, i.e. all events are returned
         */
        limit?: number;
        /**
         * Chunk size, usually not needed.
         *
         * @default  100
         */
        chunkSize?: number;
    }): AsyncIterableIterator<PeerReaction>;
```
### telegram.getReplyTo
For messages containing a reply, fetch the message that is being replied.

Note that even if a message has replyToMessage,
the message itself may have been deleted, in which case
this method will also return `null`.
**Available**: ✅ both users and bots
```ts
telegram.getReplyTo(message: Message): Promise<Message | null>;
```
### telegram.getScheduledMessages
Get scheduled messages in chat by their IDs

For messages that were not found, `null` will be
returned at that position.

**Available**: 👤 users only

@param chatId  Chat's marked ID, its username, phone or `"me"` or `"self"`
@param messageIds  Scheduled messages IDs
```ts
telegram.getScheduledMessages(chatId: InputPeerLike, messageIds: MaybeArray<number>): Promise<(Message | null)[]>;
```
### telegram.getAllScheduledMessages
Get all scheduled messages in chat

**Available**: 👤 users only

@param chatId  Chat's marked ID, its username, phone or `"me"` or `"self"`
```ts
telegram.getAllScheduledMessages(chatId: InputPeerLike): Promise<Message[]>;
```
### telegram.deleteScheduledMessages
Delete scheduled messages by their IDs.

**Available**: 👤 users only

@param chatId  Chat's marked ID, its username, phone or `"me"` or `"self"`.
@param ids  Message(s) ID(s) to delete.
```ts
telegram.deleteScheduledMessages(chatId: InputPeerLike, ids: number[]): Promise<void>;
```
### telegram.sendScheduled
Send previously scheduled message(s)

Note that if the message belongs to a media group,
the entire group will be sent, and all the messages
will be returned.

**Available**: 👤 users only

@param peer  Chat where the messages were scheduled
@param ids  ID(s) of the messages
```ts
telegram.sendScheduled(peer: InputPeerLike, ids: MaybeArray<number>): Promise<Message[]>;
```
### telegram.getWebPagePreview
Get a preview of a web page contained in the message

**Available**: 👤 users only

@param text  Text of the message, or simply the link for which the preview should be retrieved
```ts
telegram.getWebPagePreview(text: InputText): Promise<WebPageMedia | null>;
```
## Referenced mtcute input types

这些声明由生成脚本从本地 mtcute `.d.ts` 摘录，用来解释上面签名里出现的输入类型。遇到更深的嵌套类型时，按 Source 路径继续查本地 mtcute 声明。

### ForwardMessageOptions

Source: `node_modules/@mtcute/core/highlevel/methods/messages/forward-messages.d.ts`

```ts
export interface ForwardMessageOptions {
    /** Destination chat ID, username, phone, `"me"` or `"self"` */
    toChatId: InputPeerLike;
    /** When forwarding to forums, ID of the thread to forward to */
    toThreadId?: number;
    /**
     * When forwarding to a monoforum you are an admin of,
     * you **must** pass an ID of a peer you are sending the message to.
     */
    toMonoforumPeer?: InputPeerLike;
    /**
     * Whether to forward silently (also applies to caption message).
     */
    silent?: boolean;
    /**
     * If set, the forwarding will be scheduled to this date
     * (also applies to caption message).
     * When passing a number, a UNIX time in ms is expected.
     *
     * You can also pass `0x7FFFFFFE`, this will send the message
     * once the peer is online
     */
    schedule?: Date | number;
    /**
     * Whether to clear draft after sending this message (only used for caption)
     *
     * @default  `false`
     */
    clearDraft?: boolean;
    /**
     * Whether to forward without author
     */
    noAuthor?: boolean;
    /**
     * Whether to forward without caption (implies {@link noAuthor})
     */
    noCaption?: boolean;
    /**
     * Whether to disallow further forwards of this message.
     *
     * Only for bots, works even if the target chat does not
     * have content protection.
     */
    forbidForwards?: boolean;
    /**
     * Peer to use when sending the message.
     */
    sendAs?: InputPeerLike;
    /**
     * If passed, instead of sending the message, it will be saved into the
     * given quick reply shortcut (either its ID or its shortcut string).
     */
    quickReply?: number | string;
    /**
     * Whether to dispatch the forwarded messages
     * to the client's update handler.
     */
    shouldDispatch?: true;
    /**
     * Bots only: if set, allows sending up to 1000 messages per second,
     * ignoring broadcasting limits for a fee of 0.1 Telegram Stars per message.
     * The Stars will be withdrawn from the bot's balance.
     */
    allowPaidFloodskip?: boolean;
    /**
     * Whether to allow payment for messages.
     * If set, the value represents the maximum number of stars to be paid
     */
    allowPaidMessages?: tl.Long;
    /** Video timestamp to use for the forwarded video */
    videoTimestamp?: number;
}
```

### GetReactionUsersOffset

Source: `node_modules/@mtcute/core/highlevel/methods/messages/get-reaction-users.d.ts`

```ts
export type GetReactionUsersOffset = string;
```

### InputMessageId

Source: `node_modules/@mtcute/core/highlevel/types/messages/input-message-id.d.ts`

```ts
/**
 * Parameters for methods that accept a message
 * 
 * Either a message object (in `message` field), or a chat ID and a message ID
 */
export type InputMessageId = {
    chatId: InputPeerLike;
    message: number;
} | {
    message: Message;
};
```

### InputPeerLike

Source: `node_modules/@mtcute/core/highlevel/types/peers/peer.d.ts`

```ts
/**
 * Type that can be used as an input peer to most of the high-level methods. Can be:
 *  - `number`, representing peer's marked ID*
 *  - `string`, representing user's username
 *  - `"me"` and `"self"` which will be replaced with the current user/bot
 *  - Any object with `inputPeer: tl.TypeInputPeer` property
 *  - Raw TL object
 * 
 * > * Telegram has moved to int64 IDs. Though, Levin [has confirmed](https://t.me/tdlibchat/25071)
 * > that new IDs *will* still fit into int53, meaning JS integers are fine.
 */
export type InputPeerLike = 'me' | 'self' | 'some_username' | (string & {}) | number | tl.TypePeer | tl.TypeInputPeer | tl.TypeInputUser | tl.TypeInputChannel | tl.RawUser | tl.RawChannel | tl.RawChat | {
    inputPeer: tl.TypeInputPeer;
};
```

### InputReaction

Source: `node_modules/@mtcute/core/highlevel/types/reactions/types.d.ts`

```ts
/**
 * Input version of ReactionEmoji, which also accepts bare TL object
 */
export type InputReaction = string | tl.Long | tl.TypeReaction;
```

### InputText

Source: `node_modules/@mtcute/core/highlevel/types/misc/entities.d.ts`

```ts
/**
 * Type to be used as a parameter for methods that accept
 * a formatted text with entities.
 * 
 * Can be either a plain string or an object with `text` and `entities` fields.
 */
export type InputText = string | TextWithEntities;
```

### Message

Source: `node_modules/@mtcute/core/highlevel/types/messages/message.d.ts`

```ts
/**
 * A Telegram message.
 */
export declare class Message {
    readonly _peers: PeersIndex;
    /**
     * Whether the message is scheduled.
     * If it is, then its {@link date} is set to future.
     */
    readonly isScheduled: boolean;
    /**
     * Raw TL object.
     */
    readonly raw: tl.RawMessage | tl.RawMessageService;
    constructor(raw: tl.TypeMessage, _peers: PeersIndex, 
    /**
     * Whether the message is scheduled.
     * If it is, then its {@link date} is set to future.
     */
    isScheduled?: boolean);
    /** Unique message identifier inside this chat */
    get id(): number;
    /**
     * For channel posts, number of views
     *
     * `null` for service messages and non-post messages.
     */
    get views(): number | null;
    /**
     * For channel posts, number of forwards
     *
     * `null` for service messages and non-post messages, or
     * if the current user is not an admin in the channel
     */
    get forwards(): number | null;
    /**
     * For channel posts with signatures enabled, name of the post author
     */
    get signature(): string | null;
    /**
     * Whether the message is incoming or outgoing:
     *  - Messages received from other chats are incoming (`outgoing = false`)
     *  - Messages sent by you to other chats are outgoing (`outgoing = true`)
     *  - Messages to yourself (i.e. *Saved Messages*) are incoming (`outgoing = false`)
     */
    get isOutgoing(): boolean;
    /**
     * Whether this message is a service message
     */
    get isService(): boolean;
    /** Whether this message has content protection (i.e. disabled forwards) */
    get isContentProtected(): boolean;
    /**
     * Whether the message was sent by an implicit action, for example,
     * as an away or a greeting business message, or as a scheduled message
     */
    get isFromOffline(): boolean;
    /** Whether this is a silent message (no notification triggered) */
    get isSilent(): boolean;
    /** Whether there are unread media attachments in this message */
    get hasUnreadMedia(): boolean;
    /** For messages in groups chats, the rank of the sender, if any */
    get fromRank(): string | null;
    /** Whether this is a broadcast channel post */
    get isChannelPost(): boolean;
    /**
     * Whether this message was automatically sent from a scheduled message.
     *
     * **Note**: for messages sent by other users, this is always `false`.
     */
    get isFromScheduled(): boolean;
    /** If this message is a scheduled message, the repeat period in seconds */
    get scheduleRepeatPeriod(): number | null;
    /** Whether the message is pinned in the current chat */
    get isPinned(): boolean;
    /** Whether the message should be considered unedited, even if {@link editDate} ≠ null */
    get hideEditMark(): boolean;
    /**
     * If set, any eventual webpage preview should be shown on top of
     * the message instead of at the bottom.
     */
    get invertMedia(): boolean;
    /**
     * Multiple media messages with the same grouped ID
     * indicate an album or media group
     *
     * `null` for service messages and non-grouped messages
     */
    get groupedId(): tl.Long | null;
    /**
     * Same as {@link groupedId}, but is globally unique across chats.
     */
    get groupedIdUnique(): string | null;
    /**
     * Message sender.
     *
     * Usually is a {@link User}, but can be a {@link Peer}
     * in case the message was sent by an anonymous admin, anonymous premium user,
     * or if the message is a forwarded channel post.
     *
     * If the message was sent by an anonymous admin,
     * sender will equal to {@link chat}.
     *
     * If the message is a forwarded channel post,
     * sender is the channel itself.
     */
    get sender(): Peer;
    /**
     * Number of boosts applied to this {@link chat} by the sender
     */
    get senderBoostCount(): number;
    /**
     * Conversation the message belongs to
     */
    get chat(): Peer;
    /**
     * Date when the message was sent
     */
    get date(): Date;
    /**
     * Date when the message was last edited
     */
    get editDate(): Date | null;
    /**
     * If this message is a forward, contains info about it.
     */
    get forward(): MessageForwardInfo | null;
    /**
     * Whether the message is a channel post that was
     * automatically forwarded to the connected discussion group
     */
    get isAutomaticForward(): boolean;
    /**
     * Information about comments (for channels) or replies (for groups)
     */
    get replies(): MessageRepliesInfo | null;
    /**
     * For replies, information about the that is being replied to.
     *
     * Mutually exclusive with {@link replyToStory}
     */
    get replyToMessage(): RepliedMessageInfo | null;
    /**
     * For replies, information about the story that is being replied to
     *
     * Mutually exclusive with {@link replyToMessage}
     */
    get replyToStory(): tl.RawMessageReplyStoryHeader | null;
    /** Whether this message is in a forum topic */
    get isTopicMessage(): boolean;
    /**
     * Whether this message contains mention of the current user
     */
    get isMention(): boolean;
    /**
     * If non-null, this message is not actually sent, and is
     * instead inside a group of "quick reply" messages
     * under the given shortcut ID
     */
    get quickReplyShortcutId(): number | null;
    /**
     * If this message is generated from an inline query,
     * information about the bot which generated it
     */
    get viaBot(): User | null;
    /**
     * If this message was sent by a business bot on behalf of {@link sender},
     * information about the business bot.
     *
     * **Note**: only available to the business account and the bot itself.
     */
    get viaBusinessBot(): User | null;
    /**
     * Message text or media caption.
     *
     * Empty string for service messages
     * (you should handle i18n yourself)
     */
    get text(): string;
    /**
     * Message text/caption entities (may be empty)
     */
    get entities(): ReadonlyArray<MessageEntity>;
    get textWithEntities(): TextWithEntities;
    /**
     * Message action. `null` for non-service messages
     * or for unsupported events.
     *
     * For unsupported events, use `.raw.action` directly.
     */
    get action(): MessageAction | null;
    /**
     * Message media. `null` for text-only and service messages
     * and for unsupported media types.
     *
     * For unsupported media types, use `.raw.media` directly.
     */
    get media(): MessageMedia;
    /**
     * Whether this is a premium media
     * (e.g. >2gb file or fullscreen sticker)
     * that was forwarded without author by a non-premium user
     */
    get isForwardedPremiumMedia(): boolean;
    /**
     * TTL period of the message, in seconds.
     */
    get ttlPeriod(): number | null;
    /**
     * Reply markup provided with this message, if any.
     */
    get markup(): ReplyMarkup | null;
    /**
     * Whether this message can be forwarded
     *
     * `false` for service mesasges and private restricted chats/chanenls
     */
    get canBeForwarded(): boolean;
    /**
     * Whether reactions are possible on this message
     *
     * > **Note**: for non-service message this is always `true`,
     * > regardless if the message can actually be reacted to.
     */
    get canBeReacted(): boolean;
    /**
     * Reactions added to this message, if any
     */
    get reactions(): MessageReactions | null;
    /**
     * Information about fact-check added to the message, if any
     */
    get factCheck(): FactCheck | null;
    /**
     * If this message was sent with a message effect, ID of the effect
     */
    get effectId(): tl.Long | null;
    /** Whether this message has a video that is still being processed */
    get videoProcessingPending(): boolean;
    /** If this message was paid, the number of stars spent sending it */
    get sendPrice(): tl.Long | null;
    /**
     * If this message was a paid suggested post, the type of payment
     * (either `stars` or `ton`)
     */
    get suggestedPaid(): 'ton' | 'stars' | null;
    /** If this is/was a suggested post, information about it */
    get suggestedPost(): SuggestedPostInfo | null;
    /**
     * Generated permalink to this message, only for groups and channels
     *
     * @throws MtArgumentError  In case the chat does not support message links
     */
    get link(): string;
}
```

### ParametersSkip2

Source: `node_modules/@mtcute/core/highlevel/types/utils.d.ts`

```ts
export type ParametersSkip2<T> = T extends (a: any, b: any, ...args: infer P) => any ? P : never;
```

### QuoteParamsFrom

Source: `node_modules/@mtcute/core/highlevel/methods/messages/send-quote.d.ts`

```ts
export type QuoteParamsFrom<T> = Omit<NonNullable<T>, 'quoteText' | 'quoteEntities'> & {
    /**
     * Destination chat ID, username, phone, `"me"` or `"self"`
     *
     * @default  `message.chat`
     */
    toChatId?: InputPeerLike;
    /** Index of the first character to quote (inclusive) */
    start: number;
    /** Index of the last character to quote (exclusive) */
    end: number;
};
```

### SendCopyGroupParams

Source: `node_modules/@mtcute/core/highlevel/methods/messages/send-copy-group.d.ts`

```ts
export interface SendCopyGroupParams extends CommonSendParams {
    /** Destination chat ID */
    toChatId: InputPeerLike;
}
```

### SendCopyParams

Source: `node_modules/@mtcute/core/highlevel/methods/messages/send-copy.d.ts`

```ts
export interface SendCopyParams extends CommonSendParams {
    /** Target chat ID */
    toChatId: InputPeerLike;
    /**
     * New message caption (only used for media)
     */
    caption?: InputText;
    /**
     * For bots: inline or reply markup or an instruction
     * to hide a reply keyboard or to force a reply.
     */
    replyMarkup?: ReplyMarkup;
}
```
